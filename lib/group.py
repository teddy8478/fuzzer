
def div_format(msgs):
	form = {}
	for msg in msgs:
		print(msg.parts)
'''			
class group:
	def __init(self, msg, keys):
		parts = re.split(' |:|/|&|=|\r|\n|,', msg['req'])
		self.len = len(parts)
		for key in keys:
			self.(key, parts[key])
'''		
